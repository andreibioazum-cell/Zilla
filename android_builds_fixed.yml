name: 🤖 Android Builds
on:
  workflow_call:
    secrets:
      SERVICE_ACCOUNT_KEY:
        required: true
  workflow_dispatch:

env:
  SCONS_CACHE_LIMIT: 4096
  SCONS_FLAGS: >-
    target=editor
    dev_build=no
    production=yes
    scu_build=yes
    scu_limit=64
    optimize=speed
    debug_symbols=no
    separate_debug_symbols=no
    lto=none
    progress=no
    verbose=no
    warnings=none
    werror=no
    tests=no
    fast_unsafe=yes
    vsproj=no
    use_static_cpp=no
    disable_exceptions=yes
    deprecated=no
    minizip=yes
    brotli=no
    vulkan=yes
    opengl3=yes
    forward_plus_renderer=no
    engine_update_check=no
    use_volk=yes
    module_text_server_fb_enabled=yes
    swappy=no
    generate_android_binaries=yes
    builtin_msdfgen=yes
    builtin_harfbuzz=no
    builtin_icu4c=no
    builtin_graphite=no
    builtin_brotli=no
    num_jobs=8

jobs:
  build-android:
    # 8 vCPU / 32 GB (free for public repos). Same arch and compiler as the
    # default runner, so ccache/SCons caches stay fully valid.
    runs-on: ubuntu-24.04-8core
    name: Editor (SCU, arm64, optimized)
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive
      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
      - name: Restore ccache
        uses: actions/cache/restore@v6
        with:
          path: ${{ github.workspace }}/.ccache
          key: ccache-android-editor-speed|${{ runner.os }}|${{ hashFiles('**/SCsub', '**/SCU*', 'SConstruct', 'methods.py') }}
          restore-keys: ccache-android-editor-speed|${{ runner.os }}|
        continue-on-error: true
      - name: Restore Godot build cache
        uses: ./.github/actions/godot-cache-restore
        with:
          cache-name: android-editor-fast
        continue-on-error: true
      - name: Setup Python and SCons
        uses: ./.github/actions/godot-deps
      - name: Restore Godot build cache (post-deps)
        uses: ./.github/actions/godot-cache-restore
        with:
          cache-name: android-editor-fast
        continue-on-error: true
      - name: Compilation
        uses: ./.github/actions/godot-build
        with:
          scons-flags: ${{ env.SCONS_FLAGS }} arch=arm64
          platform: android
          target: editor
      - name: Cache diagnostics
        shell: bash
        run: |
          echo "=== ccache stats (end of build) ==="
          ccache -s 2>&1 || echo "ccache binary not found!"
          echo "=== cache directory sizes ==="
          du -sh .ccache .scons_cache 2>/dev/null || true
          if [ ! -d .ccache ] || [ -z "$(ls -A .ccache 2>/dev/null)" ]; then
            echo "::warning::.ccache is missing or empty — ccache is NOT caching. Check the 'Using ccache launcher:' line in the Compilation log."
          fi
      - name: Make gradlew executable
        shell: bash
        run: chmod +x platform/android/java/gradlew
      - name: Generate APK via Gradle (explicit)
        shell: bash
        run: |
          cd platform/android/java
          ./gradlew --version
          ./gradlew generateGodotEditor --info --stacktrace

          # NOTE: Diagnostics below must never fail the job.
          # `run:` blocks execute under `bash -eo pipefail`, so a command piped into
          # `head` kills the step once `head` stops reading: the producer dies on the
          # broken pipe and pipefail propagates its status (`ls` -> exit code 2).
          # Redirect to a file first, then truncate the *file* (no pipe involved).
          echo "=== APK files ==="
          find . -name "*.apk" > "$RUNNER_TEMP/apks.txt" 2>&1 || true
          head -n 100 "$RUNNER_TEMP/apks.txt" || true

          echo "=== editor/build tree (first 500 lines) ==="
          ls -R editor/build > "$RUNNER_TEMP/editor_build.txt" 2>&1 || true
          head -n 500 "$RUNNER_TEMP/editor_build.txt" || true

          echo "=== app/build tree (first 200 lines) ==="
          ls -R app/build > "$RUNNER_TEMP/app_build.txt" 2>&1 || true
          head -n 200 "$RUNNER_TEMP/app_build.txt" || true
      - name: List outputs
        shell: bash
        run: |
          echo "=== lib (tools/debug) ==="
          ls -lh platform/android/java/lib/libs/tools/debug/arm64-v8a/ || true
          echo "=== find apk/aab/so ==="
          find platform/android -type f \( -name "*.apk" -o -name "*.aab" -o -name "*.so" \) > "$RUNNER_TEMP/android_outputs.txt" 2>&1 || true
          head -n 100 "$RUNNER_TEMP/android_outputs.txt" || true
          echo "=== bin ==="
          ls -lh bin/ || true
          ls -lh bin/android_editor_builds/ || true
          echo "=== editor/build ==="
          ls -R platform/android/java/editor/build > "$RUNNER_TEMP/editor_build_full.txt" 2>&1 || true
          head -n 300 "$RUNNER_TEMP/editor_build_full.txt" || true
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: android-editor-apk
          path: |
            platform/android/java/editor/build/outputs/apk/**/*.apk
            platform/android/java/editor/build/outputs/bundle/**/*.aab
            bin/android_editor_builds/**/*.apk
            bin/android_editor_builds/**/*.aab
            bin/*.apk
            bin/*.aab
          if-no-files-found: error
      - name: Save ccache
        uses: actions/cache/save@v6
        with:
          path: ${{ github.workspace }}/.ccache
          key: ccache-android-editor-speed|${{ runner.os }}|${{ github.sha }}
      - name: Save Godot build cache
        uses: ./.github/actions/godot-cache-save
        with:
          cache-name: android-editor-fast
        continue-on-error: true
